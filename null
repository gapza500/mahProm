The provided Swift code for `VetChatView.swift` is incomplete. It cuts off in the middle of a `Text` view modifier chain:

```swift
                                    if msg.type == .text {
                                        Text(msg.text).padding().background(
```

Please provide the complete and correct Swift code for the `VetChatView.swift` file so I can overwrite it.